# Lightweight Kafka workers (no Chromium — URL producer / public URL consumer).
FROM node:20-bookworm-slim
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

CMD ["npm", "run", "kafka:url-producer"]
